version: "3.9"

services:
  backend:
    build: .
    ports:
      - "5000:5003"
    environment:
      - MONGO_URI=mongodb://mongo:27017/user-service
    depends_on:
      - mongo

  mongo:
    image: mongo:8
    ports:
      - "27018:27017"
